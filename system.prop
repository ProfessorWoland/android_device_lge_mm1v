# RIL
ro.telephony.default_network=9

# Graphics
ro.sf.lcd_density=320

# memory trim, turn off for now
ro.sys.fw.use_trim_settings=false
ro.sys.fw.empty_app_percent=50
ro.sys.fw.trim_empty_percent=100
ro.sys.fw.trim_cache_percent=100
ro.sys.fw.trim_enable_memory=1610612736

ro.config.max_starting_bg=32

# use 3 threads max for dex2oat, let it be this for now
ro.sys.fw.dex2oat_thread_count=3
